python project using docker 
Used docker build -t my-python-app . to build the Docker image.
to create project directly : mkdir python-docker this creates dedicated folder for docker and pyhton files 
